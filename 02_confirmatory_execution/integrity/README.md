# Integrity

*Archival packaging documentation.*

`chaves-confirmatorias/` (`chave-e1.json`, `chave-e2.json`) — the unblinded keys for the confirmatory E1/E2 generation (system variant, bit permutation, module-order permutation, family index, generating-θ hash), moved out of the classifier-accessible directory before analysis and opened only after scoring, per the frozen procedural-blinding protocol.

`execucao-fase5-registo.txt` — the mechanical Fase 5 execution log: preflight, generation, blinding, classifier/scale/exhaustive-equivalence runs, key-SHA verification, unblinding, single scoring run, preservation.

Epistemic status: `CONFIRMATORY`.
