# Scoring

*Archival packaging documentation.*

- `class-e1.json` — E1 classifier output (150 instances).
- `escala-e1.json`, `escala-e2.json` — scale-preservation (projection) check output.
- `equiv-agregado.json` — exhaustive representational-equivalence check, aggregated over E1 ∪ E2.
- `equiv-confirmatorio.log` — short status log of the exhaustive-equivalence run.
- `resultado-pontuacao-A.txt` — the final scoring output: `resultado_confirmatorio_A = "negativo"`, full per-candidate itemization for C1′/C2/C3.

Epistemic status: `CONFIRMATORY`. These values are never recalculated or reinterpreted anywhere in this repository.
