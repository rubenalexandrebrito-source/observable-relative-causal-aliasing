# Benchmark

*Archival packaging documentation. This directory holds no data files of its own — see below.*

`benchmark-oficial.json` (Fase 0 cost-curve output) genuinely differs between the two official gates — it is a real-world wall-clock timing measurement, not deterministic even with identical code — and is therefore consolidated alongside the rest of the gate-specific, two-gate comparison rather than kept here:

```
../dry_run/gate1_NAO_PASSA/benchmark-oficial.json    (first gate, NAO_PASSA)
../dry_run/gate2_PASSA/benchmark-oficial.json         (second gate, PASSA)
```

See `../dry_run/README.md`.
