# WS2 — Information-loss decomposition

> POST-CONFIRMATORY / EXPLORATORY

Decomposes exactly where, in the aggregation chain from pointwise response to C1′'s weak-rank decision, the discriminating causal information is destroyed: pointwise-popcount (4.2%), location/multiset invariance (24.9%), or strict within-cell arithmetic cancellation (70.9%, the dominant mode, and family 20's mode on both decisive edges) — never between aggregation blocks (proven, `B4 = 0`).

## Contents

- `WS2-INFORMATION-LOSS.md` — final report.
- `ws2_a`–`ws2_h` scripts/outputs — replay of registered cases, site-level cases, population census, family-20 site-level analysis, ordinal (L2) analysis, theorem verification, the general operator argument, and an end-to-end check against the real frozen classifier.
- `precommit-ws2-v2.txt` — pre-commitment for the second (completed) attempt.
- `tentativa-interrompida-1/` — a first attempt that was interrupted before producing a report; its outputs were audited and its scripts re-executed byte-identically as part of provenance verification (see the final report). Preserved, not deleted, per the repository's duplicate-handling rule.
- `SHAS.txt` — manifest of this workstream's own files.

## Epistemic status

`POST-CONFIRMATORY`. No correction to C1′ is proposed.
