# WS3 — Strict realized dynamics

> POST-CONFIRMATORY / EXPLORATORY

Tests whether the memory-dependent causal modulation in family 20 is realized strictly — at cells `(r,c)` actually visited by the deterministic orbit under *both* receiver-memory contexts — rather than only in the full counterfactual fiber. Result: yes, on both decisive edges (9 strict sites/edge), with observational (non-surgical) witnesses; strict realization is the population norm (80–86% of edges), not an exception.

## Contents

- `WS3-STRICT-REALIZED-DYNAMICS.md` — final report, including an audited and disclosed cosmetic labeling discrepancy in the first pass (a bit-order relabeling that does not affect any reported count).
- `ws3_lib.py`, `ws3_fam20.py`, `ws3_bateria.py`, `ws3_indep.py`, `ws3_verifica.py`, `ws3_fresh.py` and their outputs — main analysis, independent verification route, and a fresh baseline population check.
- `precommit-ws3-strict.txt`, `precommit-ws3-fresh-emenda.txt` — pre-commitments.
- `tentativa-interrompida-1/` — a first attempt interrupted before its report; audited and preserved (its scripts were adopted verbatim and reproduced byte-identically in the completed run).
- `SHAS.txt` — manifest of this workstream's own files.

## Epistemic status

`POST-CONFIRMATORY`. The original historical artifact is preserved as-is; the cosmetic discrepancy is documented, not silently corrected.
