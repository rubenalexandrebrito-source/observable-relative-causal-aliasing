# WS1 — Algebra of the L1 metric blind route

> POST-CONFIRMATORY / EXPLORATORY

Independent algebraic derivation of the necessary-and-sufficient condition for the L1 (exact cardinal) route of C1′'s metric blindness, produced under an internal firewall (no access to the prior single-agent analysis until after the derivation and its scripts were complete and hashed).

## Key result

`d₀ = d₁ ⟺ K(θ): τ = π₁∘π₀⁻¹ preserves the aggregated receiver-response weights` — independently re-derived and proven equivalent (via a 6×6 determining-set argument, rank 6, determinant −8) to the isometry formulation `τ ∈ Iso(W_M)`.

## Contents

- `WS1-ALGEBRA-L1-DRAFT-prefirewall.md` — the derivation as it stood the moment before the firewall was lifted (evidence of independence).
- `WS1-ALGEBRA-L1.md` — final report, including the post-firewall comparison against the prior single-agent analysis.
- `NOTAS-DERIVACAO.md` — derivation notes.
- `s10`–`s14` scripts/logs/outputs — validation examples, blind family-20 test, full population replay, real-classifier cross-check, pre-committed out-of-sample validation, and active refutation attempt.
- `precommit-ws1-oos.txt` — pre-committed OOS parameters.
- `SHAS.txt` — manifest of this workstream's own files.

## Epistemic status

`POST-CONFIRMATORY`. Agreement with the prior single-agent derivation is internal reproduction / independent implementation within the same research workflow, not external replication.
