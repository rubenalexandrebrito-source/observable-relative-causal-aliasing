# Coordinator / red-team

> POST-CONFIRMATORY / EXPLORATORY

The coordinator's own independent re-derivations and red-team checks, run with its own reserved seeds (`910000050`, `910000051`) and its own from-scratch implementations, used to cross-check WS1–WS5's findings before synthesis (`../../04_phase6_closure/FASE6-MULTIAGENT-SYNTHESIS.md`).

## Contents

- `coord_a_linear_toy.py` (+ `.out`) — manual toy-model verification of the blindness/aliasing distinction (illustrative, not confirmatory evidence).
- `coord_b_fam20.py` (+ `.out`) — independent reconstruction of family 20 from the blinded confirmatory table alone.
- `coord_c_fresh_II.py` (+ `.out`) — fresh System-II sample, all pre-fixed criteria checked with zero violations.
- `coord_d_fresh_III.py` (+ `.out`) — fresh System-III sample (Theorem III-1/III-2 re-verification).
- `precommit-coordinator.txt` — pre-commitment fixed before these re-executions.
- `SHAS.txt` — manifest of this workstream's own files.

## Epistemic status

`POST-CONFIRMATORY`. Internal reproduction, not external replication.
