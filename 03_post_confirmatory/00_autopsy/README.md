# Autopsy — instance `7bb0baab3a8ed7aa`

> POST-CONFIRMATORY / EXPLORATORY

First mechanistic explanation of the single C1′ confirmatory error: family 20 (System II, Stratum E2). Establishes that C1′ fuses the core `{A,B,C_AB,C_BA}` into one component (the System-III pattern) despite a real, strictly realized causal difference between the two receiver-memory contexts (4,608 point-to-point dependent sites/edge; 9 strict sites/edge at the causally sufficient `(r,c)` granularity), and derives the closed-form algebraic condition (`K(θ) ⟺ L1`, later shown ⟺ `τ ∈ Iso(W_M)`) that explains why the aggregate statistic is blind to it.

## Contents

- `FASE6-AUTOPSIA-7bb0baab3a8ed7aa.md` — the autopsy report.
- `autopsia_step1.py`, `autopsia_step3a.py`, `autopsia_step3b.py` — the analysis scripts (mechanism reconstruction, reachable-orbit distinguishability test).

Full prevalence-study code/outputs referenced by this report live in `../01_prevalence/`.

## Epistemic status

`POST-CONFIRMATORY`. No C1″ is proposed here or anywhere in this repository.
