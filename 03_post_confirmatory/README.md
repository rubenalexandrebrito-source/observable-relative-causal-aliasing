# Post-Confirmatory Analyses

> **POST-CONFIRMATORY / EXPLORATORY.** Every artifact in this directory tree was produced *after* the confirmatory result was closed (`02_confirmatory_execution/scoring/resultado-pontuacao-A.txt`, `resultado_confirmatorio_A = "negativo"`). Nothing here recalculates, reinterprets, rescues, or retroactively validates that result. No replacement criterion ("C1″") was ever formulated. The confirmatory instrument (`../01_frozen_confirmatory_instrument/`) was never modified and its `chattr +i` immutability was preserved throughout.

## What this directory answers

Given the single C1′ confirmatory error (instance `7bb0baab3a8ed7aa`, family 20, System II, Stratum E2): *why did it happen, is it a bug, how prevalent is it, and does it generalize?*

## Contents

- **`00_autopsy/`** — first mechanistic explanation of the single error.
- **`01_prevalence/`** — pre-fixed-N exploratory prevalence study (46/10,000 ≈ 0.46%).
- **`02_ws1_algebra/`** through **`06_ws5_failure_structure/`** — the five-workstream multi-agent characterization (algebraic condition for the metric route L1; information-loss decomposition; strict-realization/witness analysis; System-III temporal specificity; failure-class structure), each internally reproduced/audited, never external replication.
- **`07_temporal_horizon2/`** — pointer/synthesis of the horizon-2 (`h=2`) temporal-composition result, whose underlying files live inside `05_ws4_specificity/` (not duplicated).
- **`08_prior_art/`** — the closed adversarial prior-art audit, plus the author's own first-pass audit note.
- **`09_coordinator/`** — the coordinator/red-team's independent re-derivations, precommit, and outputs.
- **`MANIFEST.txt`, `SEEDS.txt`** (this directory) — the shared frozen-instrument manifest and seed registry that every workstream verified against before its own analysis.

## Independence taxonomy

Agreement between workstreams in this directory is **internal reproduction / independent implementation within the same research workflow** — never described as external scientific replication, which has not occurred for any result archived here.

## Epistemic status

`POST-CONFIRMATORY`, uniformly. See the root `README.md` epistemic chronology table.
